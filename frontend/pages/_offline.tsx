import React from 'react';
import ErrorBlock from '../src/components/ErrorBlock';

const Offline = () => (
  <ErrorBlock text="You are offline!" subtitle="please try later" />
);

export default Offline;
